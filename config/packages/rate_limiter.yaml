framework:
    rate_limiter:
        # Submitting is cheap for the visitor and expensive for us - it spends github.com API quota and
        # ends in a clone. Generous for a human, useless for a script.
        submission:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
